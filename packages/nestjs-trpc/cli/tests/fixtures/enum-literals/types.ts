export enum TypeEnum {
    Normal = 'Normal',
    Special = 'Special',
}

export enum Status {
    Active = 'ACTIVE',
    Inactive = 'INACTIVE',
}

export enum Priority {
    Low,
    Medium,
    High,
}
